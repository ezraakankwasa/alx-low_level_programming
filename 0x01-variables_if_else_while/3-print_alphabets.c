#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Program prints whether number is greater than 0
 * Return: 0
 */
int main(void)
{
	int x = 'A';

	while (x < '{')
	{
		if (x >= '[' && x <= '`')
		{
			continue
		}
		putchar(x);
		x++;
	}
	while (
	putchar('\n');
	return (0);
}
