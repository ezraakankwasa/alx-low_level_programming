#include "main.h"

/**
 * mul - checks for digit
 * @a: input character to be checked
 * @b: input character to be checked
 * Return: multiple of a and b
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
